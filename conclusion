sudo nano /etc/default/grub

GRUB_CMDLINE_LINUX_DEFAULT="quiet splash console=ttyS0"

sudo update-grub

Редактирование файла /etc/default/grub для добавления параметра console=ttyS0 позволит направлять вывод на последовательный порт (COM-порт) 0